-- Единый снимок DDL таблиц и колонок public-схемы (источник правды).
-- TODO: пока не собран из живой БД. Заполняется по мере ручных правок SQL:
-- после каждого применённого DDL (CREATE/ALTER TABLE, колонки, constraints)
-- фиксировать актуальное определение здесь тем же коммитом.
-- RLS-политики — в policies/, функции — в functions/, триггеры — в triggers.sql.
